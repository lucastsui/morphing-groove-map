[project]
name = "mgm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "librosa>=0.11.0",
    "mido>=1.3.3",
    "numpy>=2.4.6",
    "soundfile>=0.13.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
